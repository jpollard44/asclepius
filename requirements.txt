# Pinned to floors verified working for this app (adaptive thinking +
# output_config require a recent Anthropic SDK).
anthropic>=0.109
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.12
python-dotenv>=1.0   # load .env on startup regardless of how the server is launched
openpyxl>=3.1   # reading smart-scale body-composition .xlsx exports
pywebpush>=2.0   # VAPID-signed web push to PWA subscriptions
APScheduler>=3.10   # background scheduling of health reminders
httpx[http2]>=0.27   # APNs (HTTP/2) push delivery to the iOS app
